DB_HOST=localhost
DB_PORT=3306
DB_USER=auction_user
DB_PASSWORD=auction_pass_2024
DB_NAME=auction_db
JWT_SECRET=auction_super_secret_jwt_2024
PORT=8080

# Redis (optional — leave REDIS_HOST empty to disable cache, app falls back
# to MySQL automatically. Recommended for production.)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# DB pool tuning (defaults shown — usually no need to change)
DB_MAX_OPEN_CONNS=50
DB_MAX_IDLE_CONNS=25
DB_CONN_MAX_LIFETIME_MIN=30
DB_CONN_MAX_IDLE_MIN=10
